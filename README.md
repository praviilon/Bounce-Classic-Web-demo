# Bounce Classic Web — демо

Демонстрационная версия браузерной игры в стиле классической **Bounce**
с телефонов Nokia, реализованная согласно техническому заданию
(«Bounce Classic Web», единая версия ТЗ).

## Версия 1.1 — исправления и улучшения

По результатам тестирования исправлены следующие ошибки:

- **Батуты (зелёные платформы) не работали.** Они отрисовывались, но
  физически никак не были задействованы — шарик просто стоял на
  платформе под ними, как на обычном полу. Теперь при приземлении на
  зону батута шарик подбрасывается вверх с учётом множителя батута
  (обычный ×1.5, сильный ×2, супер ×3) и текущего состояния (обычный/
  надутый/сдутый).
- **В меню не работали Esc и Enter при выходе из «Настроек» (а также
  «Управления» и «Об игре»).** Причина: при открытии этих экранов
  менялся только видимый оверлей, а внутреннее состояние игры
  оставалось `menu`, из-за чего обработчик клавиш продолжал считать,
  что игрок всё ещё в главном меню, и полностью игнорировал Esc.
  Теперь состояние переключается корректно, а Esc и Enter одинаково
  закрывают любой из этих экранов.
- **Вентилятор не мог поднять шарик над пропастью.** Подъёмная сила
  вентилятора прибавлялась к обычной гравитации, но была слабее её,
  поэтому результирующая сила всё равно тянула шарik вниз. Теперь
  внутри зоны вентилятора гравитация не действует — её полностью
  заменяет подъёмная сила, как и было задумано.

Также уровень расширен и немного усложнён, добавлены новые элементы:

- катящийся камень (препятствие на грязевом участке);
- два маятника (качающиеся препятствия, требующие точного тайминга);
- вертикальный лифт (движущаяся платформа по вертикальной оси) —
  дополняет ранее существовавшую горизонтальную движущуюся платформу;
- вторая ломающаяся платформа подряд (более требовательный участок);
- третья контрольная точка и 10 колец вместо 6;
- пороги времени для оценки S/A/B/C пересчитаны под увеличившуюся
  длину уровня.

Все траектории прыжков и батутов проверены числовым моделированием
физики (та же формула гравитации/импульса, что и в игре), чтобы
подтвердить, что уровень действительно проходим.

Демо содержит **один полный уровень**, последовательно демонстрирующий
все механики из ТЗ: обычные/движущиеся (горизонтальные и вертикальные)/
ломающиеся платформы, лёд, грязь, шипы, катящийся камень, маятники,
три типа батутов (обычный/сильный/супер), насосы надувания и сдувания,
узкий тоннель (проходим только сдутым шариком), воду с течением (надутый
шарик плавает, обычный медленно тонет, сдутый тонет мгновенно),
вентилятор (поднимает надутый шарик над пропастью), 10 колец, три
контрольные точки, финиш со статистикой и оценкой (S/A/B/C), систему
жизней, паузу, главное меню и настройки громкости.

## Технологии

Чистый **HTML5 + CSS3 + JavaScript (ES2023)**, рендеринг на `<canvas>`
(Canvas 2D API). **Никаких внешних зависимостей и сборщиков не требуется** —
проект представляет собой статический набор файлов и открывается прямо
в браузере или после загрузки на любой веб-хостинг.

Звук реализован через встроенный **Web Audio API** (звуки генерируются
программно, аудиофайлы не требуются). Шрифт `Press Start 2P` подключается
с Google Fonts по CDN (если хостинг без интернета для шрифтов —
игра всё равно работает, просто использует запасной моноширинный шрифт).

Прогресс (лучшее время, количество колец) сохраняется в `localStorage`
браузера.

## Требования к браузеру

Протестировано на актуальных версиях **Google Chrome** и **Safari**
(desktop). Также должно работать в Firefox и Edge, поскольку используются
только стандартные веб-технологии (Canvas 2D, Web Audio API, ES2023,
`localStorage`).

## Структура проекта

```
bounce-web-demo/
├── index.html          — разметка страницы, корпус телефона, оверлеи меню
├── css/
│   └── style.css        — стилизация под экран/корпус кнопочного телефона
├── js/
│   ├── audio.js          — генерация звуков через Web Audio API
│   ├── input.js          — обработка клавиатуры (стрелки, Enter, Esc, R, P)
│   ├── save.js           — сохранение прогресса в localStorage
│   ├── physics.js        — вспомогательные функции коллизий
│   ├── ball.js            — класс шарика (состояния normal/inflated/deflated)
│   ├── level.js           — данные демо-уровня (платформы, насосы, вода и т.д.)
│   ├── game.js            — состояния экрана, физика, коллизии, рендер, HUD
│   └── main.js             — точка входа, игровой цикл
└── README.md
```

## Управление

| Клавиша | Действие |
|---|---|
| ← / → | Движение шарика |
| ↑ | Прыжок |
| ↓ | Погасить прыжок / отскок от батута |
| R | Рестарт уровня |
| Esc / P | Пауза |
| Enter | Подтверждение в меню |

Размер шарика (надутый/сдутый) меняется **только через насосы на уровне**,
свободного переключения клавишей нет — согласно ТЗ.

## Как развернуть на веб-хостинге

Игра полностью статична, поэтому подходит любой обычный хостинг
статических файлов:

1. Распакуйте архив `bounce-web-demo.zip`.
2. Загрузите **всё содержимое** папки `bounce-web-demo/` (включая
   подпапки `css/` и `js/`) в корень (или подпапку) вашего хостинга —
   например, через FTP, панель управления хостингом, GitHub Pages,
   Netlify, Vercel, Cloudflare Pages и т.п.
3. Откройте `index.html` в браузере по адресу вашего сайта.

Никакой серверной части, базы данных или сборки не требуется —
достаточно, чтобы хостинг отдавал статические файлы (HTML/CSS/JS)
по HTTP/HTTPS.

### Локальная проверка перед загрузкой

Из-за политики браузеров в отношении `file://` рекомендуется открывать
игру через локальный веб-сервер, а не двойным кликом по `index.html`:

```bash
# Python 3
cd bounce-web-demo
python3 -m http.server 8080
# затем открыть http://localhost:8080 в браузере
```

## Известные ограничения демо-версии

- Реализован только один демонстрационный уровень (полноценная игра
  по ТЗ предполагает 12 уровней и несколько миров).
- Экран «Настройки» управляет только громкостью — переназначение клавиш
  в демо не реализовано (заложено в архитектуре `InputManager` для
  дальнейшего расширения).
- Значения физики (гравитация, скорость, импульсы батутов) подобраны
  ориентировочно для демонстрации механик и могут потребовать
  дополнительной балансировки в полной версии игры.
